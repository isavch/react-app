// import {getUser} from './user';
// import { call } from 'redux-saga/effects'
// import user from '../../services/api/user';

test('User saga', () => {
  // const iterator = getUser();
  // const next = iterator.next();
  //
  // expect(next.value).toEqual(call(user));
});
